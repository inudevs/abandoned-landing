import * as React from 'react';

import Layout from '../../components/Layout';

const BranderJob: React.FC = () => {
  return (
    <Layout
      className="brander-recurit"
    />
  );
};

export default BranderJob;
