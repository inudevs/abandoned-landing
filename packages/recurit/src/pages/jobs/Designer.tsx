import * as React from 'react';

import Layout from '../../components/atoms/Layout';

const DesignerJob: React.FC = () => {
  return (
    <Layout
      className="designer-recurit"
    />
  );
};

export default DesignerJob;
